# Core
PyQt5>=5.15.0
numpy>=1.21.0
requests>=2.28.0

# Audio (optional on Windows — app falls back to mock recorder if unavailable)
pyaudio>=0.2.14

# Dev / testing
pytest>=7.0.0
pytest-cov>=4.0.0
requests-mock>=1.11.0
